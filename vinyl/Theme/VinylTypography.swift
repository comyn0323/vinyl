import SwiftUI

enum VinylTypography {
    static let friendName = Font.title3.weight(.semibold)
    static let songTitle = Font.subheadline.weight(.medium)
    static let artist = Font.footnote.weight(.regular)
    static let caption = Font.caption.weight(.regular)
}
